
Running:
 - read_file(file_name=ai_generated_project/temp_project_info.json)


Running:
 - save_file(file_name=.gitignore, contents=...)
 - save_file(file_name=Dockerfile, contents=...)
 - save_file(file_name=.dockerignore, contents=...)


Running:
 - save_file(file_name=docker-compose.yml, contents=...)
 - save_file(file_name=docker-compose.prod.yml, contents=...)
 - save_file(file_name=.github/workflows/ci-cd.yml, contents=...)
 - save_file(file_name=.github/workflows/test.yml, contents=...)


Running:
 - save_file(file_name=deployment/scripts/deploy.sh, contents=...)
 - save_file(file_name=scripts/setup.sh, contents=...)

The complete CI/CD pipeline and deployment configuration have been generated for the TaskMaster Pro project. Below is a summary of each file and what it includes:

### Project Files Created

1. **.gitignore**
   - Comprehensive ignore rules for Python and JavaScript projects to avoid committing unnecessary files.
   - Includes ignores for built assets, package managers, and IDE/editor specific files.

2. **Dockerfile**
   - Multi-stage Dockerfile built on Python 3.11-slim.
   - Installs dependencies in a separate stage to optimize the final image size.
   - Uses Uvicorn for running the FastAPI application.

3. **.dockerignore**
   - Specifies files and directories to exclude from the Docker build context, similar to `.gitignore`.
   - Helps in optimizing build times by ignoring unnecessary files.

4. **docker-compose.yml**
   - Defines services for the development environment including the app and PostgreSQL database.
   - Binds app port to local development, sets environment variables, and mounts project volumes.

5. **docker-compose.prod.yml**
   - Configuration for production environment deployment.
   - Specifies environment variable management for sensitive data like database passwords.

6. **.github/workflows/ci-cd.yml**
   - GitHub Actions workflow for continuous integration and deployment on `main` branch.
   - Includes steps for dependency installation, linting, testing, Docker image build & push, and deployment.

7. **.github/workflows/test.yml**
   - Workflow triggered on all branches except `main` for testing.
   - Covers steps for installation, linting, and testing with Pytest.

8. **deployment/scripts/deploy.sh**
   - Script to automate the deployment process in production.
   - Pulls latest images, restarts containers with new images, and cleans up old images.

9. **scripts/setup.sh**
   - Development setup script to ensure Docker and Docker-compose are installed.
   - Builds and starts the Docker environment for development.

### Instructions for Use
- Use `.gitignore` and `.dockerignore` to manage unwanted files in source control and Docker builds respectively.
- Use `docker-compose.yml` for development environments and `docker-compose.prod.yml` for deploying to production.
- GitHub Actions workflows in `.github/workflows` will automate CI/CD processes including test automation and deployment.
- Deployment and setup scripts are located in the `scripts` and `deployment/scripts` directories for easy execution of environment setups.

This setup follows DevOps best practices ensuring a smooth build, test, deploy cycle, and efficient use of resources.